# Handoff runtime template

Optional bullets to paste under **State** when relevant. Delete unused lines.

## Goal

- One line.

## State

- Done:
- In progress:
- Blocked:
- Branch / dirty tree:
- Open PR / ticket:

### Python / FastAPI backend

- Python toolchain: `uv`
- Entry / test command:
- Env names / config notes:
- `pm2 restart litreview-api` needed?:

### React / Vite frontend

- Package manager: `pnpm` (`frontend/`)
- Scripts in use:
- Dev URL / proxy:
- Production build + API restart needed?:

### PM2

- `litreview-api`:
- `litreview-ui`:
- `litreview-tunnel`:
- Last `pm2 list` notes:

## Next steps

1.
2.
3.

## Landmines

-

## Suggested skills

- (subset of: grill, prototype, architecture, research, advisor, ponytail)

## Reference artifacts

- Docs via `.cursor/docs/INDEX.md`:
- Code paths (`src/`, `frontend/src/`):
- PR / ticket:
