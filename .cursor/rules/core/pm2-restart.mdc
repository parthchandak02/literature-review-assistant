---
description: Restart PM2 after backend or frontend changes
alwaysApply: true
---

# PM2 Restart Reminder

When making many changes in one session, periodically restart PM2 services before validating tests or reporting status.

- Use exact process names: `litreview-api`, `litreview-ui`, `litreview-tunnel`.
- Quick restart (local dev default): `make pm2-restart` or `./scripts/ops_pm2.sh restart` (api + ui).
- Backend only: `./scripts/ops_pm2.sh restart --backend-only`.
- Production URL: `make pm2-restart -- --prod-ui` or `ops_pm2.sh restart --prod-ui` (build + api).
- Include tunnel: `ops_pm2.sh restart --all`.
- Before claiming local/online verification, run `pm2 list` and confirm required processes are online.
