---
description: Build phase status and test commands. Apply when asked about implementation status, adding new phases, or running acceptance criteria tests.
globs:
alwaysApply: false
---

# Build Phase Status

## Runtime Pipeline Status
Current top-level runtime checkpoint order in `src/orchestration/resume.py`:
`phase_2_search` -> `phase_3_screening` -> `phase_4_extraction_quality` ->
`phase_4b_embedding` -> `phase_5_synthesis` -> `phase_5b_knowledge_graph` ->
`phase_5c_pre_writing_gate` -> `phase_6_writing` -> `finalize`

All phases implemented and verified. See `.cursor/docs/IMPLEMENTATION_STATUS.md` and `.cursor/docs/PIPELINE.md` for acceptance criteria and runtime phase contracts.

## Test Commands
```bash
uv run pytest tests/unit -q
uv run pytest tests/integration -q
uv run python -m src.main --help
```

## Lint Commands
```bash
uv run ruff check .          # Python linting (ruff>=0.9 configured in pyproject.toml)
cd frontend && pnpm lint     # ESLint
cd frontend && pnpm typecheck  # TypeScript type check
```

When adding a new phase, update all phase-order surfaces together:
`src/orchestration/workflow.py`, `src/orchestration/resume.py`,
`src/web/app.py`, and `frontend/src/lib/constants.ts`.
In `frontend/src/lib/constants.ts`, update both the display `PHASE_ORDER`
and the resume-only `RESUME_PHASE_ORDER` when backend `PHASE_ORDER` changes.
The display `PHASE_ORDER` may include UI-only sub-phases such as
`fulltext_pdf_retrieval` that are not part of backend resume order.
Treat `RESUME_PHASE_ORDER` as the frontend surface that must stay aligned with
`src/orchestration/resume.py`.
