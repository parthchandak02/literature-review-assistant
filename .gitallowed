# git-secrets allowlist — documentation false positives (not real credentials)
Hermes agent venv
src/config/env_context
API Keys section
\.venv/bin
cloudflared/litreview
pm2/logs
env: \{ PORT
loaded at startup
in your 
restart the server
_API_KEY in
no raw secrets
safe-area-inset
from the server
env fills in
credentials in repo
Copy example
^\.[e]nv$
^\.[e]nv\.local$
^\.v[e]nv/
^v[e]nv/$
\.gitignore:[0-9]+:\.[e]nv
\.gitignore:[0-9]+:\.[e]nv\.local
\.gitignore:[0-9]+:\.v[e]nv/
\.gitignore:[0-9]+:v[e]nv/
Gemini API key is required
GET /api/config/env-keys
env-keys
credentials from the backend environment
PORT=8001
[e]nv\.example
Edit `\.[e]nv`:
# llm-provider skill (operator docs; not credentials)
llm-provider/SKILL
Never commit the local dotenv
Operator local dotenv
Using \.[e]nv
Server \.[e]nv is active
Keys in <code
configured on the server
"source": "env"
