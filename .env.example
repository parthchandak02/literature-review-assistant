# ==============================================================================
# LLM PROVIDER CONFIGURATION
# ==============================================================================
# Set LLM_PROVIDER to one of: "gemini", "perplexity"
# Default: "gemini"
LLM_PROVIDER=gemini

# ------------------------------------------------------------------------------
# Perplexity
# ------------------------------------------------------------------------------
# IMPORTANT: Perplexity has TWO separate API keys:
# 1. PERPLEXITY_API_KEY - For LLM tasks (only if LLM_PROVIDER=perplexity)
# 2. PERPLEXITY_SEARCH_API_KEY - For academic search functionality (recommended)

# For LLM tasks (only needed if LLM_PROVIDER=perplexity)
# Get API key from: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_llm_api_key_here

# For academic search (separate from LLM API key)
# Get API key from: https://www.perplexity.ai/settings/api
PERPLEXITY_SEARCH_API_KEY=your_perplexity_search_api_key_here

# Domain filtering mode for Perplexity search (optional)
# Options: "allowlist" (include only top 20 academic domains - default),
#          "denylist" (exclude non-academic domains - recommended),
#          "none" (no filtering)
# Recommended: "denylist" to capture ALL academic sources while filtering noise
PERPLEXITY_DOMAIN_FILTER_MODE=denylist


# ==============================================================================
# RESEARCH TOOL API KEYS (Optional - for enhanced web search)
# ==============================================================================

